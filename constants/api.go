package constants

const (
	SystemMessage = "You are an expert assistant that answers questions very ojectively in markdown format. Context is passed to you before the human prompt, so you can use it to answer the prompt."
)
